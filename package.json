{
  "name": "motel-korpus-tailwind",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./style.css -o ./output.css --watch --minify",
    "build": "npx @tailwindcss/cli -i ./style.css -o ./output.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18"
  }
}